# Configuración para SQL Server - Servidor dwin0293
# Variables de entorno específicas para la conexión a SQL Server

# Tipo de base de datos - cambiar a 'sqlserver' para usar SQL Server
DB_TYPE=mysql

# Configuración SQL Server
SQLSERVER_HOST=dwin0293
SQLSERVER_PORT=1433
SQLSERVER_DATABASE=sat_digital_v2
SQLSERVER_USERNAME=calidad
SQLSERVER_PASSWORD=passcalidad
SQLSERVER_ENCRYPT=false
SQLSERVER_TRUST_CERT=true
SQLSERVER_INSTANCE=

# Configuración MySQL (mantener para compatibilidad)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=sat_digital_v2
MYSQL_USERNAME=root
MYSQL_PASSWORD=

# Variables originales del sistema (mantener)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sat_digital_v2
DB_USER=root
DB_PASS=
DB_DIALECT=mysql

# Otras configuraciones
NODE_ENV=development
PORT=3001
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui_2024
JWT_REFRESH_SECRET=tu_jwt_refresh_secret_muy_seguro_aqui_2024

# Configuración de CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://localhost:3008

# Configuración de archivos
UPLOAD_PATH=uploads/
MAX_FILE_SIZE=10485760

# Configuración Ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llava

# Configuración Email (Ethereal para desarrollo)
EMAIL_SERVICE=ethereal
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=